name: Sync Tour Dates

# on:
#   schedule:
#     - cron: '0 4 * * *' # Désactivé temporairement pour sécurité
on:
  workflow_dispatch: # Garde uniquement le déclenchement manuel sécurisé

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install google-auth google-api-python-client requests

      - name: Run sync script
        env:
          GOOGLE_SERVICE_ACCOUNT_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_KEY }}
          CALENDAR_ID: ${{ secrets.CALENDAR_ID }}
        run: python sync_calendar.py

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add dates.json
          git commit -m "Auto-update tour dates from Google Calendar" || exit 0
          git push
